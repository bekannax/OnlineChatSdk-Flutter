//buildscript {
//    repositories {
//        google()
//        mavenCentral()
//        gradlePluginPortal()
//    }
//
//    dependencies {
//        classpath 'com.android.tools.build:gradle:8.6.0' // 8.5.2 8.7.3
//        classpath 'com.google.gms:google-services:4.4.3' // 4.4.2
//    }
//}


allprojects {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}